# Production build for the public GitHub Pages demo. No backend is deployed,
# so the app runs entirely on the in-browser mock backend (seeded demo data).
# Point this at a real API and set VITE_USE_MOCKS=false for a live deployment.
VITE_USE_MOCKS=true
